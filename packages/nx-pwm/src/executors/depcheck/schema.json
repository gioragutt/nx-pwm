{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Depcheck executor",
  "description": "This executor uses the `depcheck` module to scan a project's codebase and report any missing or invalid dependencies that should be in its `package.json`.",
  "type": "object",
  "properties": {
    "missing": {
      "type": "boolean",
      "description": "Check for missing dependencies",
      "default": true
    },
    "discrepancies": {
      "type": "boolean",
      "description": "Check for version discrepancies between root and project dependencies",
      "default": true
    }
  },
  "required": []
}
